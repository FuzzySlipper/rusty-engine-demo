{
  "schemaVersion": 1,
  "repository": "FuzzySlipper/rusty-engine-demo",
  "requiredChecks": ["verify"],
  "localVerification": [
    "pnpm run verify",
    "pnpm run verify:native (for renderer/host changes)",
    "pnpm run audit:boundary (for boundary-contract changes)",
    "pnpm run verify:tauri (for Tauri-relevant changes)"
  ],
  "notes": "Automatic managed reviews require the downstream verify job, which runs the lean gate (typecheck, gameplay-package drift, canonical content, TS unit tests, shell build, Rust fmt/tests/clippy). Boundary audit, native-host proof, and desktop verification are explicit on-demand commands outside the automatic workflow."
}
